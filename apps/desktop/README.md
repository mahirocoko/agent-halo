# Agent Halo Desktop

Planned desktop renderer for Agent Halo.

The first real desktop milestone should subscribe to:

```text
http://127.0.0.1:47621/events
```

and render a compact presence state derived from the protocol package.

Do not start by scraping terminal output or transcript files. Those can be fallback diagnostics later, not the primary source.
