# Halo Soft Cube mascot

Halo Soft Cube Candidate A is the production-approved Agent Halo body/identity with the production-approved Candidate C semantic action-signal layer: an original rounded four-sided pixel pet with discrete pseudo-3D highlight, front-plane, and bottom/right shadow tones. It is 2D native-grid pixel art, not a 3D/voxel render.

## Selected identity

- Human-selected family: Candidate A, Balanced Rounded Cube.
- Forms: `core`, `cat-corner`, and `sprout`.
- Source frame: `16×12` with anchor `[8,10]` and baseline `y=10`.
- Display frame: `32×24`, nearest-neighbor only.
- Body affect and the detached action/status signal animate as independent semantic layers.
- The side signal is not a tail or decorative mote. Idle/inactive hides it; active signals map grouped truthful activity.
- Vibe Island was used only to study tiny readability and low-frame motion grammar. No pixels, silhouettes, or exact timing were copied.

## Runtime assets

`apps/desktop/public/mascots/halo-soft-cube/` contains:

- 15 production-approved two-frame body strips: three forms × five presence states;
- ten two-frame grouped action/status signal strips with no idle signal;
- `manifest.json` with independent body/signal contracts, mapping, palette, hashes, provenance, and candidate status.

Canonical runtime strips use one five-tone cyan palette. CSS hue/saturation filters provide deterministic working-session variation and attention/done/error overrides while preserving tonal depth. Visible status text/glyphs remain the semantic source of truth; color is supplementary.

## Source and QA

- `source/` retains the selected master frames, exact palette-index map, selection receipt, body authoring history, smaller signal comparison source, and selected deterministic `author-action-signals-v2.py` source.
- `qa/` retains the body-state evidence plus action-signal target-size/dark/light/checker contact sheets and honest reports.
- Canonical rasters use binary alpha and contain no baked blur/glow.

## Promotion evidence

Mahiro selected Candidate A, approved deleting the legacy session cat, and accepted the body/identity after compact/row rendering, body cadence, state colors, reduced motion, deterministic forms, overflow, equal padding, native release install/restart, and live bridge checks passed. The old mote report remains historical evidence of the earlier pre-correction system. Mahiro then accepted the installed `12×12` Candidate C signals at `[26,6]` after correcting size, 4px visual gap, working sparkle, question silhouette, and clean check readability.
